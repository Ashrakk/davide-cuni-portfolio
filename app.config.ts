export default defineAppConfig({
	ui: {
		primary: "amber",
		gray: "neutral",
		icons: "all",
		button: {
			rounded: "rounded-full",
			base: "justify-center",
			default: {
				color: "amber",
				size: "xl"
			}
		}
	},
	colorMode: {
		preference: "dark"
	}
});
